;;NAME
;;2018
;;ddd
(defun name (  &key  )
  "   RETURNS    INPUT:  "
  (let
      ((x)
       )


    (cond
     (
      )
     (
      )
     (t nil))

    (loop
     for 
     do
     (let*
         ((x)
          )
     
     ;;end let,loop
     ))

    (values    )
    ;;end let, defun
    ))
;;TEST
;;